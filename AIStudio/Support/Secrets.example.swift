import Foundation

// Copy this file to `Secrets.swift` (it is git-ignored) and paste the bearer
// token that was issued with the assignment.
enum Secrets {
    static let bearerToken = "PASTE_BEARER_TOKEN_HERE"
}
